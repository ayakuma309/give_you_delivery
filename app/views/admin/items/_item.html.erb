<tr>
  <td><%= item.id %></td>
  <td><%= item.name %></td>
  <td>
    <%= link_to "編集", edit_admin_item_path(item), class: " btn btn-primary" %>
  </td>
  <td>
    <%= button_to "削除", admin_item_path(item), method: :delete , class: " btn" %>
  </td>
</tr>
